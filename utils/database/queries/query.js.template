module.exports = pool => {
  async () => {
    try {
      let [
        rows,
        fields
      ] = await pool.query('db query here;');

      return rows;
    } catch (err) {
      throw new Error(err);
    }
  }
};